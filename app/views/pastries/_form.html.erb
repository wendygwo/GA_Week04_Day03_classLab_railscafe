<%= form_for(@pastry) do |f| %>
  <%= f.collection_select :combo_id, Combo.all, :id, :cname %>
  <% if @pastry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pastry.errors.count, "error") %> prohibited this pastry from being saved:</h2>

      <ul>
      <% @pastry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
